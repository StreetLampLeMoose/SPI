{
    "version": "0.2.0",
    "configurations": [ 
        {
            "type": "pwa-chrome",
            "request": "launch",
            "name": "Launch Chrome",
            "url": "http://localhost:5500", // Adjust the port if needed
            "webRoot": "${workspaceFolder}",
            "sourceMaps": true
        }
    ]
}